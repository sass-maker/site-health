## 1. Skill foundation

- [x] 1.1 Initialize the `design-engineering` parent and four child skill folders with skill-creator and generated `agents/openai.yaml` metadata
- [x] 1.2 Author the parent routing contract and shared boundaries with `design-workflow` and Impeccable
- [x] 1.3 Add a dependency-free read-only toolchain diagnostic used by the rendering skills

## 2. Focused workflows

- [x] 2.1 Author `design-inspiration` with current-source verification, attribution, anti-copying guidance, and receipt handoff
- [x] 2.2 Author `component-pattern-mine` with comparison, full-state, accessibility, responsive, and implementation handoff guidance
- [x] 2.3 Author `web-3d-pipeline` with rendering-tier selection, asset provenance, optimization, fallback, interaction, and performance guidance
- [x] 2.4 Author `creative-web-effects` with purposeful motion, SVG, Canvas, shader, scroll, pointer, reduced-motion, and performance guidance

## 3. Fleet integration

- [x] 3.1 Route specialized jobs from `design-engineering` and reference the router from `design-workflow`
- [x] 3.2 Expose the parent through `agent-stack.sh` and update Fleet skill inventories without exposing each child separately
- [x] 3.3 Add focused tests for routing completeness, metadata, references, diagnostics, and integration boundaries

## 4. Validation and delivery

- [x] 4.1 Run skill-creator quick validation for the parent and four child skill folders
- [x] 4.2 Run the diagnostics command in fixture and real-project modes plus the focused Node tests
- [x] 4.3 Run strict OpenSpec validation, relevant existing design-workflow tests, and `git diff --check`
- [x] 4.4 Review the isolated diff, commit and push the branch, and open a pull request that closes GitHub issue #157

## 5. DesEngs refinement

- [x] 5.1 Add `shape`, `audit`, `opportunities`, and `vocabulary` modes to `creative-web-effects` with mode-specific outputs and mutation boundaries
- [x] 5.2 Add temporary code-based comparison probes and cleanup requirements to `design-inspiration`
- [x] 5.3 Add DesEngs to the source map as a non-authoritative discovery feed
- [x] 5.4 Extend focused tests and rerun skill, OpenSpec, capability, design-workflow, and whitespace validation
- [x] 5.5 Commit and push the refinement to PR #158 and confirm its required checks pass
