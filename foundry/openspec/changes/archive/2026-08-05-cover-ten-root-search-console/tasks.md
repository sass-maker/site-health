## 1. Contract and target derivation

- [x] 1.1 Add a pure, fail-closed union of public metric projects and validated root-search targets.
- [x] 1.2 Prove the union retains the 27-project policy while covering all ten roots and preserving catalog IDs.

## 2. Collection and projection

- [x] 2.1 Make the read-only Search Console collector use the shared union and allow every resulting project ID in the private ledger.
- [x] 2.2 Make only the Google Search dashboard projection include the supplemental root targets.
- [x] 2.3 Add focused regression tests for collector target coverage and dashboard rows.

## 3. Delivery

- [x] 3.1 Update operator documentation and PROJECT_STATUS with the shipped contract.
- [x] 3.2 Run focused tests, Fleet tests, strict OpenSpec validation, and diff checks.
- [x] 3.3 Archive the OpenSpec change, commit, push, open a linked pull request, review CI, and merge when green.
