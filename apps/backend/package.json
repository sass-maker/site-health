{
  "name": "site-health-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Private data and API backend for Site Health",
  "scripts": {
    "serve": "node scripts/server.mjs serve",
    "test": "node --disable-warning=ExperimentalWarning --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
