# Anchor Hub acknowledgement qualification — 8 September 2026

Source `a32e94a2c5dd415ea96dfa51e24ad53763694459`, build 23, pins PersonalSyncKit `e52fc1cffbb86b4a04f10ec2799f5bb7ed024b17`. Anchor deliberately exports finished-session summaries without importing Hub history; iCloud owns bidirectional planner data. The explicit sync callback retains that boundary.

The regression blocks the outbox file write after a synthetic server accepts a queued mutation. Against the old committed dependency, the app reports zero pending exports although the durable outbox still contains one. With the repaired dependency, it reports one pending export and a failure receipt; reopening sync and restoring the write destination retries the identical idempotent mutation successfully. No remote history is inserted into the local planner, and the existing payload assertions exclude private notes.

The main checkout has a pre-existing editable SwiftPM dependency pointing to the local shared Hub checkout. Its first passing test already used the repaired source and is not baseline evidence. That override and workspace link were preserved. An isolated checkout reproduced the failed assertion using the committed remote pin, then passed the new regression and all 186 package tests using the repaired remote pin. `Package.resolved` was generated there and copied back. Managed XcodeBuildMCP logs identify the baseline as `swift_package_test_2026-09-08T16-04-59-508Z_pid50013_97158318.log`, the corrected regression as `swift_package_test_2026-09-08T16-05-33-263Z_pid50013_a39ec6a6.log`, and all-package verification as `swift_package_test_2026-09-08T16-05-57-189Z_pid50013_21239771.log`.

[Hosted native review 34249040267](https://github.com/Significant-Hobbies/anchor/actions/runs/34249040267) is running on this exact source. It must pass shared checks, the complete Mac and iPhone UI suites, and the Watch build before this build receives native qualification. The workflow's final gate checks actual outcomes despite intermediate continue-on-error steps.

Build 23 has not been installed or distributed. Phone build 22 and Mac build 21 remain the last verified installations. Production CloudKit project fields, notarization, physical use and signed-in cross-device behavior remain unqualified; old locked-phone and schema observations were not revalidated here. [Issue 51](https://github.com/Significant-Hobbies/anchor/issues/51) retains release work, and [Hub issue 155](https://github.com/Significant-Hobbies/significanthobbies/issues/155) tracks remaining consumer audit. No provider settings, credentials or owner records changed.
