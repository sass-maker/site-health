{
  "$schema": "fleet.lint-parity-config.v1",
  "deliberateDivergences": {
    "app-health": "Keep the passing flat ESLint and Prettier setup: it covers the mixed TypeScript workspace while the independent Go SDK remains governed by gofmt, go test, and go vet.",
    "fleet-workspace": "Keep Fleet Ops under the aggregate workspace contract: the registered foundry/ops component has no standalone package boundary, while root Fleet scripts are governed by component, policy, registry, and full Fleet contract tests.",
    "karte": "Biome keeps a custom ruleset and lineWidth 80; preserve until an owner-specific migration reviews its current style contract.",
    "knowledge-base": "Keep the repository's existing multi-surface boundaries: the root is a Blume and Knip documentation workspace, the nested Next.js app owns its ESLint configuration, and the Cloudflare Worker owns its separate checks.",
    "pace": "Keep the macOS product on its native Swift and Xcode build and test boundary while the root Blume package validates documentation; there is no shared JavaScript application root to lint.",
    "posttrainllm": "Keep the model factory on its language-native Python, Rust, benchmark, and evaluation gates while the nested Astro and WebGPU browser owns separate type, test, size, and build checks; there is no coherent root JavaScript lint surface.",
    "research-papers": "Keep the Python-first research platform on its configured Ruff and pytest boundary while the nested Astro reader owns a separate build and agent-surface validation path.",
    "setline": "Keep the passing framework-owned Next.js ESLint configuration so its Core Web Vitals and TypeScript rules remain coupled to the current Next.js and Vinext stack."
  }
}
