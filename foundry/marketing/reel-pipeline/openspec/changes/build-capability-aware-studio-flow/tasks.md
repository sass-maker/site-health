## 1. Production Catalog and Contracts

- [x] 1.1 Add a server-owned recipe catalog with normalized owners, engines, spend classes, readiness, defaults, and requirements
- [x] 1.2 Add optional project, idea, recipe, and recipe-options persistence while preserving legacy records
- [x] 1.3 Add focused contract tests for every recipe mapping and backward-compatible normalization

## 2. Planner APIs and Actions

- [x] 2.1 Expose projects, project-scoped ideas, and the production catalog through Studio API endpoints
- [x] 2.2 Validate and save a selected production plan as a versioned Marketing Studio brief
- [x] 2.3 Derive truthful edit, build or continuation, preview, and Postiz preparation actions from brief state
- [x] 2.4 Add focused API tests for ordered planning, invalid selections, and terminal action gates

## 3. Ordered Dashboard Flow

- [x] 3.1 Build the project, idea, recipe, and bounded-options planner using the existing Studio visual language
- [x] 3.2 Group and compare recipes by outcome, spend posture, runtime, owner, and readiness without invented prices
- [x] 3.3 Wire saved plans into the existing detailed editor, build or continuation path, production preview, and Postiz preparation flow
- [x] 3.4 Add accessible loading, empty, validation, blocked, and responsive states while retaining the advanced brief editor

## 4. Verification and Design Review

- [x] 4.1 Run the smallest focused contract and API tests, then the relevant Studio test suite
- [x] 4.2 Validate the OpenSpec change strictly and run repository diff checks
- [x] 4.3 Capture browser evidence at required widths and complete the preserve-mode design review receipt

## 5. Agent Arsenal Consolidation

- [x] 5.1 Add one versioned, secret-free arsenal manifest for workflow, recipe, and Studio-tool decision metadata
- [x] 5.2 Make the existing capability evaluator, production planner, and Tools UI consume the canonical manifest without changing stable ids or behavior
- [x] 5.3 Add a read-only arsenal assembler that joins projects, render modes, runtime readiness, automation policies, guardrails, and next actions with source provenance
- [x] 5.4 Expose the same filterable arsenal schema through `GET /studio/arsenal` and `npm run factory -- arsenal`

## 6. Agent Contract Verification

- [x] 6.1 Add manifest integrity, drift, filter, read-only API, and CLI contract tests
- [x] 6.2 Document the single discovery surface and the explicit plan, execute, review, and Postiz boundaries for AI operators
- [x] 6.3 Run focused agent-arsenal tests, the relevant Studio suite, strict OpenSpec validation, docs validation, and diff checks

## 7. Variant and Delivery Contracts

- [x] 7.1 Expand every finite select and boolean option into stable bounded recipe variants and expose them through the arsenal API and CLI
- [x] 7.2 Persist the selected variant id and normalized values while retaining separate duration and unbounded input fields
- [x] 7.3 Add explicit final-video, preview, and continuation delivery metadata and reserve Ready for locally executable MP4 output
- [x] 7.4 Add contract tests for all 48 current variants, deterministic ids, ordering, blockers, and Auto eligibility

## 8. Final Local Video Outputs

- [x] 8.1 Encode HTML composition timelines into vertical H.264/AAC MP4 artifacts while retaining the HTML preview
- [x] 8.2 Compose Blender scene plates into a timed vertical H.264/AAC MP4 while retaining manifests, hashes, and plate evidence
- [x] 8.3 Probe Chrome, FFmpeg, and Blender requirements before marking the affected variants Ready
- [x] 8.4 Add deterministic adapter tests and one real local canary for each final-video path

## 9. Fleet Console Combination Selector

- [x] 9.1 Populate the existing format control with grouped recipe variants from the arsenal instead of a hard-coded recipe list
- [x] 9.2 Keep all variants selectable for inspection and show delivery, spend, runtime, missing input, and blocker states without color-only meaning
- [x] 9.3 Make Auto choose only a ready final-video variant and submit the stable variant id plus normalized option values
- [x] 9.4 Preserve prompt, selection, loading, failure, preview, and final-video result states at 390, 768, and 1440 pixels

## 10. Completion Verification

- [x] 10.1 Run focused variant, catalog, adapter, Studio API, and Console build checks
- [x] 10.2 Run the full Reel Pipeline suite, strict OpenSpec validation, readiness smoke, and diff checks
- [x] 10.3 Capture browser evidence at required widths, complete critique and audit, and update the preserve-mode design receipt

## 11. Explore Gallery and Visual Variety

- [x] 11.1 Add a secret-free gallery registry and read-only API that reports playable, baseline, imported, external, and unavailable sample states
- [x] 11.2 Serve registered gallery MP4s by stable id with byte-range support and focused traversal/range tests
- [x] 11.3 Add `/marketing/explore-gallery` inside the existing Fleet Console shell with style-family filters, lazy playable media, truthful provenance, and exact preset handoff
- [x] 11.4 Make the selected HTML, ASCII, and Blender visual options materially affect renderer output and add focused adapter contracts
- [x] 11.5 Replace backend-first showcase ordering with a broader style-first local sample set and keep weak Blender/Grok fixtures labelled as baselines rather than premium examples
- [x] 11.6 Validate the revised OpenSpec, focused API/adapter tests, Fleet Console build, range playback, responsive evidence, and preserve-mode design review
