name: Cloudflare Resilience Audit

on:
  workflow_dispatch:


concurrency:
  group: cloudflare-resilience-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  reusable:
    uses: sass-maker/workflows/.github/workflows/cloudflare-resilience.yml@main
