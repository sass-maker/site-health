# Tasks — studio factory line

- [x] 1.1 `src/studio/quality.js` (probe + scoring) + tests
- [x] 1.2 `IdeaStore.updateIdea` + workflow `ideaId` + quality.json in summary + tests
- [x] 2.1 `src/studio/packet.js` publish packets + tests
- [x] 2.2 `src/studio/factory.js` + `scripts/factory.js` CLI + tests
- [x] 3.1 API routes renders-list/render-file (whitelist) + UI Renders panel + server tests
- [x] 4.1 Live proof: plan → produce one kokoro render with packet + quality
- [x] 4.2 Full npm test; docs; PROJECT_STATUS; archive; commit + push
