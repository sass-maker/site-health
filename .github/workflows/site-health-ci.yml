name: Site Health CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: dashboard-ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  check:
    uses: sass-maker/workflows-and-skills/.github/workflows/site-health-ci.yml@fabaa599ed9518deacaeab0ba332e25f3402e682
