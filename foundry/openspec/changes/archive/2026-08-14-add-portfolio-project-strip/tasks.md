- [x] Add the package scaffold, public types, and generated Fleet catalog.
- [x] Implement the component with optional revalidation, validation, and
      current-project filtering.
- [x] Add responsive, themeable, accessible marquee styles with reduced-motion
      behavior.
- [x] Add package documentation, tests, and package-local agent instructions.
- [x] Add the cached SassMaker `/projects.json` endpoint with CORS and edge
      cache headers.
- [x] Run the package check and verify the generated catalog matches the
      canonical registry.
