{
  "name": "@fleet/public-directory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "homepage": "https://sassmaker.com",
  "description": "Static public product directory generated from Fleet's privacy-checked projection.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "typecheck": "astro sync && tsc --noEmit",
    "check": "npm run typecheck && npm run build",
    "deploy": "npm run check && npx wrangler@4.114.0 pages deploy dist --project-name saas-maker-home --branch main"
  },
  "dependencies": {
    "astro": "5.12.0",
    "typescript": "5.8.3"
  },
  "devDependencies": {}
}
