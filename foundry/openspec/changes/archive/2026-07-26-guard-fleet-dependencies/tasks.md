## 1. Skill Contract

- [x] 1.1 Initialize the `guard-dependencies` skill with script resources and agent UI metadata
- [x] 1.2 Author the concise pre-install review, approval, Bundlephobia, and post-change validation workflow

## 2. Deterministic Guard

- [x] 2.1 Implement read-only repository and Fleet comparison commands with stable human and JSON output
- [x] 2.2 Implement precise npm direct-dependency deltas and conservative cross-ecosystem manifest/lockfile detection
- [x] 2.3 Implement optional time-bounded exact-version Bundlephobia lookup
- [x] 2.4 Add focused tests for clean, added, removed, changed, invalid, opaque, strict, Fleet-deduplication, and lookup-normalization behavior

## 3. Fleet Integration

- [x] 3.1 Expose the standalone skill through the Fleet agent-stack installer
- [x] 3.2 Update compact Fleet skill discovery and dependency-policy documentation

## 4. Verification

- [x] 4.1 Run guard tests, skill validation, capability-catalog doctor, OpenSpec strict validation, and diff checks
- [x] 4.2 Install refreshed skill links and run the read-only guard across the active local Fleet
- [x] 4.3 Archive the completed OpenSpec change and verify the resulting main specification
