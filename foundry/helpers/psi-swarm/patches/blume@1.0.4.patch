diff --git a/dist/cli/index.js b/dist/cli/index.js
index 8c6ca7ce8c0df7c882be6ab396626f9004094cde..b4fdce5f88f00b85e53f9f635f49b162c9ba7ae4 100755
--- a/dist/cli/index.js
+++ b/dist/cli/index.js
@@ -7897,6 +7897,10 @@ export default defineConfig({
   },
   devToolbar: { enabled: false },
   vite: {
+    // Mermaid's parser family is lazy-loaded and its largest shared language
+    // chunk is about 650 kB. Keep Vite's warning threshold aligned with that
+    // known split while Blume's explicit client-asset budget remains the gate.
+    build: { chunkSizeWarningLimit: 700 },
     plugins: [tailwindcss(), prerenderDepsPlugin(), serverAppResolvePlugin()],
     // Mermaid (lazy-loaded client-side for diagrams) statically imports dayjs as
     // CJS (\`dayjs/dayjs.min.js\`). In dev, an un-pre-bundled dependency is served
diff --git a/src/astro/templates.ts b/src/astro/templates.ts
index aa9a00b76e480e56c9f3d3435fbd82db799be3c2..4e0592e0652d5bc1d916999db03dbaa1be93801d 100644
--- a/src/astro/templates.ts
+++ b/src/astro/templates.ts
@@ -438,6 +438,10 @@ export default defineConfig({
   },
   devToolbar: { enabled: false },
   vite: {
+    // Mermaid's parser family is lazy-loaded and its largest shared language
+    // chunk is about 650 kB. Keep Vite's warning threshold aligned with that
+    // known split while Blume's explicit client-asset budget remains the gate.
+    build: { chunkSizeWarningLimit: 700 },
     plugins: [tailwindcss(), prerenderDepsPlugin(), serverAppResolvePlugin()],
     // Mermaid (lazy-loaded client-side for diagrams) statically imports dayjs as
     // CJS (\`dayjs/dayjs.min.js\`). In dev, an un-pre-bundled dependency is served
