# Tasks — kokoro local voice

- [x] 1.1 `scripts/setup-kokoro.sh` + `.gitignore` + npm alias; run setup on this machine
- [x] 1.2 `scripts/kokoro_tts.py` batch synth helper
- [x] 1.3 `src/adapters/kokoro.js` (readiness, batch synth, elevenlabs-shaped wrapper) + tests
- [x] 2.1 Lesson pipeline TTS provider selection + tests
- [x] 2.2 `src/adapters/kokoro-compose.js` + registration (video-brief, pipeline, workflow, api, cli, render-modes.json) + tests
- [x] 3.1 Live proof: real wav synth + full `--engine kokoro` faceless render
- [x] 3.2 Full `npm test` green; docs + PROJECT_STATUS; archive; commit + push
