## 1. Scope And Contracts

- [x] 1.1 Create and link the implementation GitHub issue before starting independently shippable code work ([#156](https://github.com/sass-maker/fleet-workspace/issues/156)).
- [x] 1.2 Add versioned workflow, stage, dependency-revision, character, cast-instance, soundtrack, and evidence schemas with fixture-backed validation tests.
- [ ] 1.3 Add readiness tests that distinguish private experiment, local execution, review, and distribution without weakening Postiz gates.
- [x] 1.4 Add exact-model dispatch tests proving that an unavailable selected executor fails before generation and never silently switches to Forge or another model.

## 2. Editable Workflow Engine

- [x] 2.1 Compile existing normalized briefs and Night Out recipes into the registered brief-to-review stage graph while preserving existing brief and production identifiers.
- [x] 2.2 Persist stage inputs, outputs, status, dependency revisions, runtime owner, receipts, and errors in the existing local Studio data boundary.
- [ ] 2.3 Implement explicit single-stage run, retry, quick auto-advance, pause, checkpoint reuse, and transitive downstream invalidation.
- [x] 2.4 Reject unknown or prompt-authored action identifiers and recheck dependencies and capability readiness immediately before execution.
- [ ] 2.5 Add focused tests for manual progression, quick-mode pause, failed-stage retry, and upstream edit invalidation.

## 3. Character Directory And Cast

- [x] 3.1 Implement versioned reusable character records and project/workflow queries without rewriting historical production evidence.
- [x] 3.2 Implement cast instances with workflow-specific role, wardrobe, expression, and continuity overrides.
- [x] 3.3 Compile character revisions, prompt constraints, and hashed visual references into each applicable scene-generation request.
- [x] 3.4 Enforce source posture, adult-age, consent, and real-person-likeness eligibility at cast assignment and pre-generation validation.
- [ ] 3.5 Add tests for reusable edits, immutable historical receipts, per-workflow overrides, named-IP private posture, and mature-likeness rejection.

## 4. Voice Intake

- [x] 4.1 Reuse the existing browser microphone pattern to implement explicit start/stop capture, recording status, local upload, and transcript review.
- [x] 4.2 Implement a local transcription adapter that probes WhisperKit first and the repository-supported MLX Whisper path second, with no implicit remote fallback.
- [x] 4.3 Preserve captured audio for retry when transcription fails and keep typed workflow creation available.
- [ ] 4.4 Add tests for permission denial, unavailable runtime, successful transcript normalization, transcript edits, and proof that transcription never starts generation.

## 5. Soundtrack Sources And Mixing

- [x] 5.1 Implement the normalized soundtrack interface and migrate the existing procedural Night Out bed to a visibly labeled `procedural-draft` adapter.
- [x] 5.2 Implement `owned-local` ingestion with supported-format validation, probing, hashing, and operator-provided rights posture.
- [x] 5.3 Implement `platform-sound` references, timing instructions, and silent upload masters without downloading or embedding commercial audio.
- [ ] 5.4 Add trim, offset, loop, fades, gain, and narration-ducking controls to the common mix specification and retain every transform in the export manifest.
- [ ] 5.5 Add soundtrack tests covering source-lane exclusivity, commercial-audio non-embedding, mix transforms, audio-stream probing, and duration evidence.

## 6. Generated-Music Canary

- [x] 6.1 Add a pinned ACE-Step preflight script that checks host architecture, memory, disk estimate, Python, official source revision, and license without installing source or model weights.
- [x] 6.2 After the green host preflight, install the pinned official MLX runtime in the ignored engine directory, verify the no-weight HTTP server in forced-offline mode, and retain a sanitized success receipt.
- [x] 6.3 Stop and present the exact 10.1 GB official ACE-Step 1.5 model set, hashes/source revision, target path, and MIT license for explicit operator approval before download.
- [x] 6.4 Only after approval, download the pinned weights and generate a 10–15 second instrumental canary with prompt, BPM, seed, runtime, duration, and file evidence.
- [ ] 6.5 Probe and human-review the canary, then register the generated soundtrack adapter only if it passes; otherwise leave the lane blocked with the retained receipt.
- [ ] 6.6 Add tests for unsupported music controls, multiple variations, explicit selection, and failure-closed behavior when runtime or weights are absent.

## 7. Mature Local Proof

- [x] 7.1 Add `mature-enabled` workflow validation for fictional identity, age 25 or older, consent-compatible scenes, and disallowed ambiguity, coercion, incest, bestiality, minors, and real-person sexual likenesses.
- [x] 7.2 Compile validated adult-age and consent constraints into the local visual-generation manifest without claiming that a selected model will accept every eligible prompt.
- [x] 7.3 Run the already installed WAI profile through the registered local executor to create one actual, playable private mature proof with fictional adults, including model hash, cast revisions, prompts, source frames, encode receipt, and quality evidence.
- [x] 7.4 Show the proof in Productions with a private-review label and confirm all distribution actions remain blocked until normal evidence is complete.
- [ ] 7.5 Add positive and negative boundary tests plus a regression test that private work does not require Fleet branding or publish rights.

## 8. Studio Experience

- [x] 8.1 Start a preserve-mode Fleet design-workflow run and capture the current Create experience at 390, 768, and 1440 pixels before editing UI.
- [x] 8.2 Add the Talk/Type composer, editable transcript review, quick/manual choice, and visible workflow stages without changing primary navigation.
- [ ] 8.3 Add searchable character-directory and cast controls with continuity, source, likeness, and mature-readiness status.
- [ ] 8.4 Add the soundtrack editor with source-lane selection, generated-music controls and variations, mix controls, runtime status, and rights-specific blockers.
- [x] 8.5 Ensure successful export selects the artifact in the existing single-player Productions view and shows workflow, cast, model, soundtrack, quality, and private/distribution evidence.
- [x] 8.6 Verify keyboard operation, visible focus and status, no color-only state, and no horizontal page scrolling at 390, 768, and 1440 pixels.

## 9. Validation And Owner Review

- [x] 9.1 Run the smallest focused test files after each implementation group, then the relevant Studio test suite and `git diff --check`.
- [x] 9.2 Complete the Fleet design-review receipt with before/after browser evidence, critique and audit floors, zero unresolved P0/P1 issues, and a passing project check.
- [ ] 9.3 Obtain owner `keep` or delegated feedback for the workflow UI before claiming the visual work complete.
- [ ] 9.4 Update product documentation and `PROJECT_STATUS.md` only with shipped truth after implementation and acceptance; keep remaining work in GitHub Issues.
