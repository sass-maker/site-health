## 1. Confirm current public truth and baseline

- [x] 1.1 Verify the generated maintained/past product projection is current and record what “latest things” can truthfully include.
- [x] 1.2 Capture the current catalog plus attached learning pane at 390, 768, and 1440 pixels and create a `preserve` design-review receipt.

## 2. Refresh shipped content

- [x] 2.1 Add a dated SaaS Maker changelog entry for the verified token-world release with concrete shipped details.
- [x] 2.2 Keep homepage and agent-readable product counts/links derived from the canonical public projection; add no private or dirty workspace truth.

## 3. Rebuild the learning composition

- [x] 3.1 Remove the learning pane from the catalog grid so maintained product rows occupy one coherent wall.
- [x] 3.2 Add a separate semantic editorial chapter after the catalog with title, description, publication date, reading time, and one reading action.
- [x] 3.3 Integrate the chapter into the workshop material system and verify responsive reading order at 390, 768, and 1440 pixels.

## 4. Lazy-load the globe runtime

- [x] 4.1 Move the Three.js scene into a focused client module without changing token data, scene appearance, pulse behavior, or lifecycle guarantees.
- [x] 4.2 Add a one-shot near-viewport loader that dynamically imports the scene, keeps the CSS/HTML fallback immediate, and fails safely.
- [x] 4.3 Exercise before-boundary, after-boundary, repeated intersection, reduced-motion, save-data, import failure, missing-WebGL, context loss, no-script, offscreen, hidden, and teardown behavior.
- [x] 4.4 Measure initial and lazy chunk delivery and confirm Three.js is absent from initial execution.

## 5. Verify and prepare release

- [x] 5.1 Run public projection checks, focused tests, typecheck, production build, and strict OpenSpec validation.
- [x] 5.2 Capture after evidence, run independent critique/detector assessments, fix all P0/P1, polish/audit, and complete the design receipt.
- [x] 5.3 Present the result for owner `keep`, `close`, `wrong-lane`, or `delegated` feedback and prepare a scoped release without deploying unless requested.
