name: Site Health CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: dashboard-ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  check:
    uses: sass-maker/saas-maker/.github/workflows/site-health-ci.yml@c2b0828686d76af52834228642278ff36d33c550
