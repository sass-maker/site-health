// PSI Swarm supports Node 22–24. Its installed native SQLite module uses the
// Node 24 ABI; project and portfolio runs must use the same supported runtime.
export const PSI_NODE_VERSION = '24.20.0';
